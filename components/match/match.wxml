<!--<template name="loginTemplate">-->
  <view class="match" >
    <view class="match-input">
      <input  type="text" bindinput="_search" />
    </view>
    <scroll-view scroll-y class="match-list" wx:if="{{matchList.length>0}}">
      <block wx:for="{{matchList}}" wx:key="{{item.id}}">
        <view class="match-item"> {{item.value}} </view>
      </block>
    </scroll-view>
  </view>


<!--</template>-->